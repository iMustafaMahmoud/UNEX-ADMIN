export type Package = {
  _id: string;
  currency: string;
  name: string;
  description: string;
  icon: string;
  duration: number;
  cost: number;
  active: boolean;
  createdAt: string;
  updatedAt: string;
};